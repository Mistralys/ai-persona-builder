# NOTE: Content templates in this suite reference {{custom_global_var}} with no default value.
# Tests using this fixture MUST supply custom_global_var via BuildConfig.variables (or
# SuiteConfig.variables) — otherwise the marker will remain unresolved and emit a warning.
default_version: '2.0.0'
author: integration-test-author
